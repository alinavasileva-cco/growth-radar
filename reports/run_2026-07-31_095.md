# Growth Radar — RUN GR-20260731-095

**Дата:** 31.07.2026  
**Тип:** нулевой технический RUN / попытка восстановления канонической базы  
**Новые компании:** 0  
**Outreach:** не готовился и не отправлялся

## Результат

Перед новым содержательным поиском повторно проверена обязательная точка целостности после RUN 093/094. Подтверждённая карточка `GRM-117` («ТоталТехКом») существует в `data/increments/leads_2026-07-31_093.csv`, но по состоянию на начало RUN отсутствует в `data/leads_master.csv`.

Для безопасной интеграции добавлен одноразовый workflow `.github/workflows/sync-grm117.yml`. Он должен:

1. проверить совпадение схем master и increment;
2. проверить отсутствие дубля Lead ID и ИНН;
3. добавить ровно одну строку `GRM-117`;
4. проверить итоговые 117 строк;
5. закоммитить изменение master.

Коммит с workflow создан, однако фактический второй commit от GitHub Actions в рамках RUN не подтверждён. Поэтому синхронизация master **не считается выполненной**, новый поиск компаний не запускался, метрики базы не увеличивались.

## Фактическое состояние

- канонический master: 116 компаний до подтверждения action-коммита;
- подтверждённый increment, ожидающий интеграции: 1 (`GRM-117`);
- новых компаний в RUN 095: 0;
- обращений: 0.

## Блокер

Доступный GitHub-коннектор позволяет заменить файл только полным содержимым; безопасно получить и перезаписать большой `leads_master.csv` без риска потери строк в текущем runtime не удалось. Запуск workflow также не дал подтверждённого commit SHA изменения master в пределах RUN.

До появления подтверждённого commit SHA, в котором `GRM-117` фактически присутствует в `data/leads_master.csv`, содержательный поиск и добавление новых карточек остановлены.